# safe to commit because it contains examples/placeholders, not actual credentials.

POSTGRES_USER=your_postgres_user
POSTGRES_PASSWORD=your_postgres_password
POSTGRES_DB=postgres

AUTH_DATABASE_URL=postgresql://your_postgres_user:your_postgres_password@localhost:5432/auth_db