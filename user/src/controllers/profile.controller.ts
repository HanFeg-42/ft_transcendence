import { Router, Request, Response } from 'express';
import { prisma } from '../prisma';


// app.get("/profile", getUserProfile );
// app.patch("/profile", updateUserProfile );

export function getUserProfile(res, req, next) {

}

export function updateUserProfile(res, req, next) {
    
}